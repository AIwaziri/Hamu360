# assets

Shared static assets (shared icons, logos, fonts) used across multiple web parts.

Web-part-specific assets (e.g. a single web part's welcome image) stay colocated under that web part's own `assets/` folder, as scaffolded by the SPFx generator — only put something here once a second web part needs it too.
